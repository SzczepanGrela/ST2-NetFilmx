@model IEnumerable<NetFilmx_Service.Dtos.Category.CategoryListDto>

<h1>Categories</h1>

<ul>
@foreach (var category in Model)
{
    <li>
        <a href="@Url.Action("Details", new { categoryId = category.Id })">@category.Name</a>
    </li>
}
</ul>
