@model IEnumerable<NetFilmx_Service.Dtos.User.UserListDto>

<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <title>Add Users to Series</title>
</head>
<body>
    <h1>Add Users to Series: @ViewBag.SeriesName</h1>

    <form asp-action="AddUsers" method="post">
        <input type="hidden" name="seriesId" value="@ViewBag.SeriesId" />
        <ul>
            @foreach (var user in Model)
            {
                <li>
                    <input type="checkbox" name="userIds" value="@user.Id" />
                    @user.Username
                </li>
            }
        </ul>
        <button type="submit" class="btn btn-primary">Add Selected Users</button>
    </form>

</body>
</html>
