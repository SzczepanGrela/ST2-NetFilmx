@model NetFilmx_User.Models.ViewModels.PurchaseHistoryViewModel

@{
    ViewData["Title"] = "Purchase History";
}

<!-- Profile Header -->
<div class="profile-header">
    <div class="container">
        <div class="row">
            <div class="col-12">
                <h1 class="profile-title">🛒 Purchase History</h1>
                <p class="profile-subtitle">View your purchased movies and series</p>
            </div>
        </div>
    </div>
</div>

<!-- Profile Content -->
<div class="container">
    <div class="row">
        <!-- Profile Sidebar -->
        <div class="col-lg-3 col-md-4">
            <div class="profile-sidebar">
                <div class="profile-avatar">
                    <img src="/images/default-avatar.png" alt="Profile Avatar" class="avatar-img">
                    <h4>@User.Identity.Name</h4>
                </div>
                
                <nav class="profile-nav">
                    <a href="/Profile" class="nav-item">
                        <i class="fas fa-user"></i> Profile Info
                    </a>
                    <a href="/Profile/Purchases" class="nav-item active">
                        <i class="fas fa-shopping-bag"></i> Purchase History
                    </a>
                    <a href="/Profile/Favorites" class="nav-item">
                        <i class="fas fa-heart"></i> My Favorites
                    </a>
                    <a href="/Profile/Settings" class="nav-item">
                        <i class="fas fa-cog"></i> Settings
                    </a>
                </nav>
            </div>
        </div>
        
        <!-- Purchase History Content -->
        <div class="col-lg-9 col-md-8">
            <div class="profile-content">
                <div class="content-header">
                    <h2>Purchase History</h2>
                    <div class="purchase-stats">
                        <span class="stat-item">
                            <strong>@(Model.VideoPurchases?.Count ?? 0)</strong> Videos
                        </span>
                        <span class="stat-item">
                            <strong>@(Model.SeriesPurchases?.Count ?? 0)</strong> Series
                        </span>
                        <span class="stat-item">
                            <strong>$@(Model.TotalSpent.ToString("F2"))</strong> Total
                        </span>
                    </div>
                </div>
                
                <!-- Video Purchases -->
                @if (Model.VideoPurchases?.Any() == true)
                {
                    <div class="purchases-section">
                        <h3>🎬 Video Purchases</h3>
                        <div class="purchases-grid">
                            @foreach (var purchase in Model.VideoPurchases)
                            {
                                <div class="purchase-card">
                                    <div class="purchase-thumbnail">
                                        <img src="/images/video-placeholder.jpg" alt="Video Thumbnail">
                                        <div class="purchase-overlay">
                                            <a href="/Video/Details/@purchase.VideoId" class="btn btn-primary btn-sm">
                                                ▶️ Watch Now
                                            </a>
                                        </div>
                                    </div>
                                    <div class="purchase-info">
                                        <h5>Video Title</h5>
                                        <p class="purchase-price">$9.99</p>
                                        <p class="purchase-date">
                                            Purchased: @purchase.PurchaseDate.ToString("MMM dd, yyyy")
                                        </p>
                                    </div>
                                </div>
                            }
                        </div>
                    </div>
                }
                
                <!-- Series Purchases -->
                @if (Model.SeriesPurchases?.Any() == true)
                {
                    <div class="purchases-section">
                        <h3>📺 Series Purchases</h3>
                        <div class="purchases-grid">
                            @foreach (var purchase in Model.SeriesPurchases)
                            {
                                <div class="purchase-card">
                                    <div class="purchase-thumbnail">
                                        <img src="/images/series-placeholder.jpg" alt="Series Thumbnail">
                                        <div class="purchase-overlay">
                                            <a href="/Series/Details/@purchase.SeriesId" class="btn btn-primary btn-sm">
                                                📺 View Series
                                            </a>
                                        </div>
                                    </div>
                                    <div class="purchase-info">
                                        <h5>Series Title</h5>
                                        <p class="purchase-price">$9.99</p>
                                        <p class="purchase-date">
                                            Purchased: @purchase.PurchaseDate.ToString("MMM dd, yyyy")
                                        </p>
                                    </div>
                                </div>
                            }
                        </div>
                    </div>
                }
                
                <!-- No Purchases -->
                @if ((Model.VideoPurchases?.Count ?? 0) == 0 && (Model.SeriesPurchases?.Count ?? 0) == 0)
                {
                    <div class="no-purchases">
                        <div class="text-center">
                            <div class="empty-icon">🛒</div>
                            <h3>No purchases yet</h3>
                            <p>You haven't purchased any videos or series yet. Start exploring our collection!</p>
                            <div class="empty-actions">
                                <a href="/Category" class="btn btn-primary">Browse Categories</a>
                                <a href="/Series" class="btn btn-outline-primary">View Series</a>
                            </div>
                        </div>
                    </div>
                }
            </div>
        </div>
    </div>
</div>

<style>
/* Purchase History Styles */
.profile-header {
    background: linear-gradient(135deg, #e50914 0%, #000000 100%);
    color: white;
    padding: 3rem 0;
    margin-bottom: 2rem;
}

.profile-title {
    font-size: 2.5rem;
    font-weight: 700;
    margin-bottom: 0.5rem;
}

.profile-subtitle {
    font-size: 1.2rem;
    opacity: 0.9;
}

.profile-sidebar {
    background: #2a2a2a;
    border-radius: 8px;
    padding: 2rem;
    margin-bottom: 2rem;
}

.profile-avatar {
    text-align: center;
    margin-bottom: 2rem;
}

.avatar-img {
    width: 100px;
    height: 100px;
    border-radius: 50%;
    object-fit: cover;
    border: 3px solid #e50914;
    margin-bottom: 1rem;
}

.profile-avatar h4 {
    color: #ffffff;
    margin-bottom: 0.5rem;
}

.profile-nav {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
}

.nav-item {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.75rem 1rem;
    color: #e5e5e5;
    text-decoration: none;
    border-radius: 6px;
    transition: all 0.3s ease;
}

.nav-item:hover {
    background: #444;
    color: #ffffff;
    text-decoration: none;
}

.nav-item.active {
    background: #e50914;
    color: #ffffff;
}

.profile-content {
    background: #2a2a2a;
    border-radius: 8px;
    padding: 2rem;
}

.content-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 2rem;
}

.content-header h2 {
    color: #ffffff;
    margin: 0;
}

.purchase-stats {
    display: flex;
    gap: 1rem;
}

.stat-item {
    color: #aaa;
    font-size: 0.9rem;
}

.purchases-section {
    margin-bottom: 3rem;
}

.purchases-section h3 {
    color: #ffffff;
    margin-bottom: 1.5rem;
    font-size: 1.5rem;
}

.purchases-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
    gap: 1.5rem;
}

.purchase-card {
    background: #333;
    border-radius: 8px;
    overflow: hidden;
    transition: transform 0.3s ease;
}

.purchase-card:hover {
    transform: translateY(-5px);
}

.purchase-thumbnail {
    position: relative;
    height: 180px;
    overflow: hidden;
}

.purchase-thumbnail img {
    width: 100%;
    height: 100%;
    object-fit: cover;
}

.purchase-overlay {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: rgba(0, 0, 0, 0.8);
    display: flex;
    align-items: center;
    justify-content: center;
    opacity: 0;
    transition: opacity 0.3s ease;
}

.purchase-card:hover .purchase-overlay {
    opacity: 1;
}

.purchase-info {
    padding: 1rem;
}

.purchase-info h5 {
    color: #ffffff;
    margin-bottom: 0.5rem;
    font-size: 1rem;
}

.purchase-price {
    color: #e50914;
    font-weight: 600;
    margin-bottom: 0.5rem;
}

.purchase-date {
    color: #aaa;
    font-size: 0.8rem;
    margin: 0;
}

.no-purchases {
    text-align: center;
    padding: 4rem 2rem;
    color: #aaa;
}

.empty-icon {
    font-size: 4rem;
    margin-bottom: 1rem;
}

.no-purchases h3 {
    color: #ffffff;
    margin-bottom: 1rem;
}

.empty-actions {
    margin-top: 2rem;
}

.empty-actions .btn {
    margin: 0 0.5rem;
}

@@media (max-width: 768px) {
    .content-header {
        flex-direction: column;
        gap: 1rem;
        text-align: center;
    }
    
    .purchase-stats {
        justify-content: center;
    }
    
    .purchases-grid {
        grid-template-columns: 1fr;
    }
    
    .profile-nav {
        flex-direction: row;
        overflow-x: auto;
        gap: 1rem;
        padding-bottom: 1rem;
    }
    
    .nav-item {
        white-space: nowrap;
        min-width: fit-content;
    }
}
</style>